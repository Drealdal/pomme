#!/usr/bin/python
import os,sys,time;
class pomme_rpcc:

    def __init__(self, ip=inet_aton('127.0.0.1'), port=1234)
    	self.ip = ip;
	self.port = port;
    def __del__(self)
    	pass
    def sync_all(self, func_name, argnum, arg)
    	

